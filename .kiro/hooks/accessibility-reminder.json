{
  "name": "Accessibility Reminder",
  "version": "1.0.0",
  "description": "Reminds to check accessibility when editing component files",
  "when": {
    "type": "fileEdited",
    "patterns": ["**/*.component.ts", "**/*.html"]
  },
  "then": {
    "type": "askAgent",
    "prompt": "A component file was just edited. Quick accessibility check: Does this change affect keyboard navigation, ARIA attributes, or focus management? If yes, remind about testing with keyboard only and updating ARIA states."
  }
}
